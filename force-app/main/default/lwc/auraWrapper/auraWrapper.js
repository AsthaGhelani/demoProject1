import { LightningElement } from 'lwc';

export default class AuraWrapper extends LightningElement {
    // You can handle load events or custom logic here
    handleLoad() {
        console.log('Aura component wrapper loaded successfully');
    }
}
